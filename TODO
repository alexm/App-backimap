TODO
====

* Extract message MIME parts, headers and IMAP flags.
* Export messages to standard format (e.g. mbox, Maildir)
* Add support for pushing messages to an IMAP server.
* Add an IMAP server interface.
